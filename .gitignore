# Build output (regenerated by `npm run build`)
dist/

# Runtime deps (regenerated by `npm install`)
node_modules/

# Test coverage
coverage/

# npm pack output
*.tgz

# Local env overrides — copy .env.example instead
.env
.env.local

# MCPB build artifacts (see .mcpbignore at the plugin root)
*.mcpb
